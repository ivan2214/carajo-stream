---
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
---

<section class="w-full border-t py-12 md:py-24 lg:py-32">
  <div class="container px-4 md:px-6">
    <div
      class="flex flex-col items-center justify-center space-y-4 text-center"
    >
      <div class="space-y-2">
        <h2 class="font-bold text-3xl tracking-tighter sm:text-4xl md:text-5xl">
          Únete a la comunidad CarajoStream
        </h2>
        <p
          class="mx-auto max-w-[700px] text-gray-500 md:text-xl/relaxed lg:text-base/relaxed xl:text-xl/relaxed dark:text-gray-400"
        >
          Mantente informado sobre nuestro contenido y las últimas noticias
          sobre Javier Milei.
        </p>
      </div>
      <div class="w-full max-w-sm space-y-2">
        <form class="flex space-x-2">
          <Input
            className="max-w-lg flex-1"
            placeholder="Ingresa tu email"
            type="email"
          />
          <Button type="submit">Suscribirse</Button>
        </form>
      </div>
    </div>
  </div>
</section>
