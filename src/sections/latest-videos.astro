---
import Container from "@/components/container.astro";
import { Button } from "@/components/ui/button";
import VideoButton from "@/components/VideoButton.astro";
import type { CategorizedVideos } from "@/types";
import { Play } from "lucide-react";

const { videos } = Astro.props as CategorizedVideos;
---

<section id="videos" class="w-full py-12 md:py-24 lg:py-32">
  <Container>
    <h2
      class="mb-8 font-bold text-3xl tracking-tighter sm:text-4xl md:text-5xl text-right"
    >
      Últimos Videos
    </h2>
    <div class="grid grid-cols-1 gap-6 md:grid-cols-2 lg:grid-cols-3">
      {
        videos?.map((video) => (
          <div class="group relative overflow-hidden rounded-lg shadow-lg">
            <img
              src={video.thumbnails.standard.url}
              alt={`Video thumbnail ${video.title}`}
              width={400}
              height={200}
              class="w-full object-cover"
            />
            <div class="p-4">
              <h3 class="mb-2 font-bold">{video.title}</h3>
              <p class="text-gray-600 text-sm">{video.publishedAt}</p>
            </div>
            <VideoButton url={video.url} className="bottom-0 right-0 p-4" />
          </div>
        ))
      }
    </div>
  </Container>
</section>
